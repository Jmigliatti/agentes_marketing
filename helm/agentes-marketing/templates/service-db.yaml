apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  selector:
    app: db
  ports:
    - protocol: TCP
      port: 5432 # Expor a porta 5432
      targetPort: 5432 # Direcionar para a porta 5432 do container
  type: ClusterIP